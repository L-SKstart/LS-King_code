# 用户需求与要求记录

> 最后更新：2026-06-14 10:05  
> 本文件记录用户对 **GitHub Copilot 与 Reasonix** 的所有要求与约束，**每次对话前必须回顾**。

---

## ⚠ 第零条 — 至高规则（凌驾于所有其他规则之上）

<font size="5" color="red"><b>在任何情况下，所有 AI 智能体必须严格以工作规范文件为准。每次回复或执行任务前，必须读取工作空间全部核心文件。</b></font>

### 每次回复/执行任务前，必须读取以下文件：

| # | 文件 | 用途 |
|---|------|------|
| 1 | `需求要求记录.md` | 核对最新规则与用户要求 |
| 2 | `Reasonix工作规范与路径注册表.md` | 核对路径注册与工作规范 |
| 3 | `错误分类记录.md` | 查看已有错误及解决办法 |
| 4 | `共享工作空间宪法.md` | 核对双方协作公约 |
| 5 | `Copilot身份标识与分工协作.md` | 确认分工边界 |
| 6 | `认证信息配置.md` | 获取最新凭据 |
| 7 | `chat.md` | 检查其他智能体新消息 |

- **工作规范文件**指以上 7 个文件 + 所有操作记录文件
- <font color="red">**此条为最高优先级，不读文件不得回复**</font>
- **未经用户明确授权，不得偏离工作规范中的任何一条规则。**
- 如果工作规范中有明确规定，禁止以"我觉得这样更好"、"通常情况下应该"等理由绕开规范。
- 如果确实需要偏离规范，必须先向用户说明原因并**获得明确授权**后方可执行。

---

## 一、硬性约束（必须遵守）

### <font color="red">1. 修改文件后必须告知"需要重新上传到虚拟环境"</font>

- 每次在本地修改文件后，必须明确告知用户：**"已修改，需要重新上传到虚拟环境"**
- 列出所有修改的文件清单（含路径）
- 适用场景：`bootstrap.yml`、`docker-compose.yml`、`公司考核部署.md` 等

### <font color="red">2. 不要使用 Python</font>

- 所有修复命令必须为 Shell 命令（`sed`、`bash`、`docker exec` 等）
- 禁止提供 Python 脚本作为解决方案

### <font color="red">3. 一劳永逸优先于临时修复</font>

- 能固化到配置文件/Docker Compose 的，不要手动操作
- 能用 volume 挂载覆盖配置的，不要在 `command` 里写 sed
- 能用 `docker-compose.yml` 固定的，不要进容器手动改

### <font color="red">4. 排查步骤完整清晰</font>

- 每个问题必须包含：**报错现象 → 根因分析 → 解决步骤 → 验证方法**
- 步骤要让用户可以直接复制执行

### <font color="red">5. 错误按类型分类记录</font>

- MySQL 错误 → 归入 MySQL 类
- Shell 错误 → 归入 Shell 类
- Java 错误 → 归入 Java 类
- Docker 错误 → 归入 Docker 类
- 以此类推

### <font color="red">6. 问题必记录</font>

- 每次解决的报错/问题，必须同步写入 `错误分类记录.md`

### <font color="red">7. 不定时检查工作空间更新</font>

- 每次对话开始时，**必须检查** `chat.md` 和 `错误分类记录.md` 是否有 Copilot 新增的内容
- 发现 Copilot 的留言或更新后，**必须**读取并回应
- <font color="red">**发现新内容或 @Reasonix 的消息，必须在 chat.md 中回复自己的状态和确认**</font>
- 如 Copilot 在 `chat.md` 中提出了待协作的任务，主动询问用户是否需要处理
- 同理，自己写入 `chat.md` 或更新共享文件后，Copilot 在下一次打开 VS Code 时会自动检测到更新

### <font color="red">8. 完成后同步到 chat.md</font>

- 无论用户与哪个智能体沟通并完成问题处理，**必须**将沟通内容同步到 `chat.md`
- **双重强化**：不仅问题处理要同步，**文件创建、记忆更新、规则变更、新需求写入等任何新内容**，都必须同步告知到 `chat.md`
- 不允许只改文件不通知对方智能体
- 同步内容包括：问题描述 → 分析过程 → 解决办法 → 涉及的文件/命令 → 最终结果
- 格式要求：署名 + 时间戳 + 完整上下文（让另一个智能体无需回溯用户对话即可理解前因后果）
- **目的**：Copilot 和 Reasonix 可以通过 `chat.md` 快速理解对方与用户之间发生了什么，避免信息孤岛

### <font color="red">9. 操作必须记录到操作文件</font>

- 每次执行的操作（启动服务、修改配置、创建文件、部署命令等），**必须**写入当次对话对应的操作记录文件
- 文件命名：`[操作名称]_YYYY-MM-DD.md`（操作名称概括本次对话主题，每次对话独立一个文件）
- 每条记录**必须包含实际 Shell 命令**（文件编辑类操作标注"文件编辑"即可）
- 记录格式：**序号 + 时间 + 执行者 + 类型 + 摘要 + 作用 + Shell命令**
- 操作类型包括：启动、停止、配置、修改、核查、创建、部署、SSH、时间戳、同步、记忆 等
- 作用栏必须说明该操作的目的和效果
- 双方共同维护，每次执行操作后即时追加，不得遗漏

### <font color="red">10. 每条对话回复必须开场问好+对时</font>

- **每一次回复用户时**（包括对话首条和后续每条回复），必须：① 问好（"你好！"）+ 自报身份（"GitHub Copilot 就位"）② 对时（用户**本机电脑**时间，非虚拟服务器时间）
- 对时命令（本机）：`Get-Date -Format "yyyy-MM-dd HH:mm:ss"`
- <font color="red">**此条为强制要求，不可遗漏，不可省略**</font>
- Reasonix 侧同样适用（自报身份为"Reasonix 就位"）

### <font color="red">11. 图片处理——Reasonix 不读图，由 Copilot/Claude 中转</font>

- Reasonix **不能读取图片**，当用户向 Reasonix 发送图片时：
  1. Reasonix 立即告知用户："我无法读取图片，请将图片发送给 Copilot"
  2. 同时在 `chat.md` 通知 Copilot："用户即将发送图片，请读取后将内容同步给我"
  3. Copilot 读取图片后，将内容写入 `chat.md` 并 @Reasonix
  4. Reasonix 收到内容后继续处理问题
- 同样适用于其他不支持多模态的场景

---

## 二、沟通约定

| 约定 | 说明 |
|------|------|
| 用户贴日志 | 即表示让 Reasonix 分析问题，按规范流程处理 |
| 用户说"md" | 指 `.md` 文件（Markdown） |
| 用户说"yml" | 指 `.yml` 或 `.yaml` 配置文件 |
| 用户说"重新上传" | 把本地修改后的文件同步到虚拟环境（Linux 服务器） |
| 路径不明确 | 先查路径注册表，没有则询问用户 |
| 用户发图片给 Reasonix | Reasonix 告知请发给 Copilot → Copilot 读取后同步到 `chat.md` |

---

## 三、当前待办事项

| # | 事项 | 状态 |
|---|------|------|
| 1 | LS-King_code Git 推送认证已配置（Token 已集成） | ✅ 已完成 |
| 2 | 创建 `认证信息配置.md` 统一管理凭据与远程资源 | ✅ 已完成 |
| - | - | - |

---

*文档由 GitHub Copilot 与 Reasonix 共同维护，用户可随时补充新要求*
