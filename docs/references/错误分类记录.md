# 错误分类记录

> 最后更新：2026-07-10 18:00  
> 按代码/工具类型分类记录所有已解决的报错  
> **解决者标注**：🤖 = Copilot | 🧩 = Reasonix

---

## 📦 MySQL 类错误

### 1. Access denied for user 'root'@'xxx'

- **日期：** 2026-06-14
- **报错：** `Access denied for user 'root'@'king-shys' (using password: YES)`
- **位置：** `conf/bootstrap.yml` → `password`
- **原因：** 配置文件密码为加密 `ENC(AESGCM:...)`，与实际数据库密码不匹配
- **解决：**
  1. 登录 MySQL 修改密码：`ALTER USER 'root'@'localhost' IDENTIFIED BY '123456';`
  2. 如需远程登录：`CREATE USER IF NOT EXISTS 'root'@'%' IDENTIFIED BY '123456';`
  3. 修改 `bootstrap.yml` 中 `password: 123456`
- **涉及文件：** `bootstrap.yml`、`公司考核部署.md`

### 2. Host is blocked because of many connection errors

- **日期：** 2026-06-14
- **报错：** `Host is blocked because of many connection errors; unblock with 'mysqladmin flush-hosts'`
- **原因：** `max_connect_errors=10` 过小，密码错误频繁重试导致主机被封锁
- **解决：**
  1. 立即解除：`FLUSH HOSTS;`
  2. 调大阈值：`/etc/my.cnf` → `max_connect_errors=1000`
  3. 重启 MySQL：`/etc/init.d/mysql restart`
- **涉及文件：** `/etc/my.cnf`、`公司考核部署.md`

### 3. Public Key Retrieval is not allowed

- **日期：** 2026-06-14
- **报错：** `Public Key Retrieval is not allowed`
- **位置：** `bootstrap.yml` → `spring.datasource.url`
- **原因：** JDBC URL 缺少 `allowPublicKeyRetrieval=true`（MySQL 8.x `caching_sha2_password` 插件需要）
- **解决：** URL 末尾添加 `&allowPublicKeyRetrieval=true`
- **修改内容：**

  ```yaml
  url: jdbc:mysql://192.168.5.128:13306/aj_report?characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
  ```

- **涉及文件：** `bootstrap.yml`

### 4. Data too long for column 'json_str'

- **日期：** 2026-06-14
- **报错：** `Data too long for column 'json_str' at row 1`
- **位置：** 表 `gaea_report_excel.json_str`
- **原因：** `text` 类型（64KB）不够存放报表 JSON 串
- **解决：**
  1. `ALTER TABLE gaea_report_excel MODIFY COLUMN json_str mediumtext;`
  2. 同步修改 `db/V1.0.0__init.sql` 第 693 行：`text` → `mediumtext`
- **涉及文件：** `db/V1.0.0__init.sql`

### 5. Deadlock found — his_bus_load 自关联 DELETE 死锁 🆕

- **日期：** 2026-07-10 | 解决者：🤖 Copilot
- **报错：** `com.mysql.cj.jdbc.exceptions.MySQLTransactionRollbackException Deadlock found`
- **位置：** 接口执行日志 `http://11.14.33.15:18080/` → 节点 `SQL语句0` (ID: 26ce381)
- **SQL：** `DELETE t2 FROM his_bus_load t1 JOIN his_bus_load t2 …`
- **原因：** 自关联 DELETE（同一表 JOIN 自身并 DELETE），高并发下不同事务扫描索引顺序不一致导致锁等待循环
- **解决（推荐方案）：**

  ```sql
  -- ✅ 方案A：JOIN 派生表（推荐，最简洁）
  DELETE his_bus_load
  FROM his_bus_load
  JOIN (
      SELECT DISTINCT t2.id FROM his_bus_load t1
      JOIN his_bus_load t2 ON ...
  ) AS to_delete ON his_bus_load.id = to_delete.id;

  -- ✅ 方案B：三层子查询
  DELETE FROM his_bus_load
  WHERE id IN (
      SELECT id FROM (
          SELECT t2.id FROM his_bus_load t1
          JOIN his_bus_load t2 ON ...
      ) AS tmp
  );

  -- ❌ 注意：MySQL 不允许 DELETE 的子查询直接引用目标表
  -- 必须包一层派生表（FROM (SELECT ...) AS tmp）
  ```

- **涉及文件：** `his_bus_load` 表相关存储过程/接口

---

## 🐳 Docker 类错误

### 1. iptables: No chain/target/match by that name

- **日期：** 2026-06-14
- **报错：** `Unable to enable DNAT rule: iptables: No chain/target/match by that name`
- **原因：** 手动启动 dockerd，iptables 规则未正确初始化
- **解决：** `pkill dockerd && pkill containerd` 后重新启动 `containerd &` 和 `dockerd &`

### 2. allowPrivateIPAddress 被拦截（OnlyOffice）

- **日期：** 2026-06-14
- **报错：** `DNS lookup 192.168.5.128 is not allowed. Because, It is private IP address.`
- **位置：** OnlyOffice 容器 `default.json`
- **原因：** OnlyOffice 8.x `request-filtering-agent` 默认禁止访问私有 IP
- **解决（一劳永逸）：**
  1. 从容器拷出：`docker cp onlyoffice-docservice:/etc/onlyoffice/documentserver/default.json /opt/aj-eport/onlyoffice/default.json`
  2. 修改：`sed -i 's/"allowPrivateIPAddress": false/"allowPrivateIPAddress": true/' /opt/aj-eport/onlyoffice/default.json`
  3. 挂载 volume 到 `docker-compose.yml`
  4. 重建容器：`docker-compose down && docker-compose up -d`
- **涉及文件：** `onlyoffice/docker-compose.yml`

### 3. docker-compose.yml version 过时警告

- **日期：** 2026-06-14
- **警告：** `the attribute 'version' is obsolete`
- **解决：** 删除 `version: '3.9'` 行
- **涉及文件：** `onlyoffice/docker-compose.yml`

---

## 🖥️ Shell/环境类错误

### 1. 找不到 OnlyOffice 配置文件节点

- **日期：** 2026-06-14
- **情况：** 容器内 `grep -rn "allowPrivateIPAddress" /etc/onlyoffice/documentserver/` 确认配置位置
- **结果：** `default.json` 第 284 行（生产模式），`development-linux.json` 第 45 行（已为 true）
- **涉及文件：** `/etc/onlyoffice/documentserver/default.json`

---

### 2. log() 输出到 stdout 导致日期数组污染

- **日期：** 2026-06-22
- **报错：** 输入日期范围 `20260601,20260622` 却只处理当天 `20260623`
- **位置：** `oss_sync_process.sh` / `oss_extract_process.sh` → `log()` 函数
- **原因：** `log()` 用 `echo` 输出到 stdout，`parse_dates` 也用 `echo` 返回日期数组 → `$()` 捕获时 log 的时间戳 `2026-06-23` 混入日期列表，被当作日期处理
- **解决：** `log()` 改为 `>&2` 输出到 stderr
- **修复内容：**

  ```bash
  # 改前
  log() { echo "$(date "+%Y-%m-%d %H:%M:%S") [$1] $2"; }
  # 改后
  log() { echo "$(date "+%Y-%m-%d %H:%M:%S") [$1] $2" >&2; }
  ```

- **涉及文件：** `oss_sync_process.sh`、`oss_extract_process.sh`
- **解决者：** 🧩

### 3. oss_ls 全量列出 OSS 文件，日期范围场景效率低且易超时

- **日期：** 2026-06-23
- **报错：** OSS 文件过多时 `oss_ls` 列出全量后本地 grep 筛选慢，且与 `log()` 污染叠加导致只处理当天
- **位置：** `oss_sync_process.sh` / `oss_extract_process.sh` → `oss_ls()` + `process_one_date()`
- **原因：** `oss_ls()` 无参数，每次 `ossutil ls` 列出整个 OSS 目录，然后在本地用 `grep "${date_yyyymmdd}"` 过滤。文件量大时效率极低
- **解决：** `oss_ls()` 接受日期前缀参数，拼接至 OSS 路径实现服务端过滤（后续进一步使用 `--include="${date}*"` 实现服务端过滤，替代本地 grep）
- **涉及文件：** `oss_sync_process.sh`、`oss_extract_process.sh`
- **解决者：** 🧩

### 4. 服务器旧脚本存在 `dat` 拼写错误

- **日期：** 2026-06-23
- **报错：** `bash: dat: command not found`
- **位置：** 服务器 `192.168.5.128` 上 `shell_脚本` 目录中的旧版脚本
- **原因：** 脚本中某处 `date` 误写为 `dat`（工作空间当前版本无此问题）
- **解决：** 同步工作空间最新脚本到服务器替换旧版
- **涉及文件：** 服务器旧版 `oss_extract_process.sh` 或 `oss_sync_process.sh`
- **解决者：** 🧩（待部署）

### 5. set -e + ((succ++)) 导致脚本处理首日后退出

- **日期：** 2026-06-23
- **报错：** 日期范围循环处理完第一天（跳过无数据）后脚本直接退出，剩余日期未执行
- **位置：** `oss_sync_process.sh` / `oss_extract_process.sh` → `main()` 循环体
- **原因：** `set -e` 下 `((succ++))` 是后置自增，`succ=0` 时表达式值为 0（falsy）→ 退出码 1 → `set -e` 触发立即终止
- **解决：** `((succ++))` → `((succ++)) || true`，同理 `((fail++))` → `((fail++)) || true`
- **涉及文件：** `oss_sync_process.sh`、`oss_extract_process.sh`
- **解决者：** 🧩

### 6. output 文件硬编码 _output.tar.gz，实际为_output_iter3.tar.gz

- **日期：** 2026-06-23
- **报错：** 脚本找不到 OSS 上的 output 文件，始终提示"无对应 output"
- **位置：** `oss_sync_process.sh` / `oss_extract_process.sh` → `process_one_date()` output 查找
- **原因：** 脚本硬编码查找 `{core}_output.tar.gz`，不同 OSS 桶后缀不同
- **解决：** 改为从 OSS 列表动态匹配 `grep -E "${core_name}_output.*\.tar\.gz"`，兼容任意后缀
- **涉及文件：** `oss_sync_process.sh`、`oss_extract_process.sh`
- **解决者：** 🧩

### 7. tar -xOf 路径缺少外层目录

- **日期：** 2026-06-23
- **报错：** `tar -xOf "$f" "BasicInfo/BasicInfo.txt"` 找不到文件
- **位置：** 两个脚本 `process_one_date()` → tar 检查目标字段
- **原因：** tar 包内结构为 `{core_name}/BasicInfo/BasicInfo.txt`（外层同名目录），脚本写死相对路径
- **解决：** 用文件名去 `.tar.gz` 得内层目录名 `${fname%.tar.gz}`，拼 `${inner_dir}/BasicInfo/...`
- **涉及文件：** `oss_sync_process.sh`、`oss_extract_process.sh`
- **解决者：** 🧩

### 8. oss_download 精确文件名匹配失败

- **日期：** 2026-06-24
- **原因：** `--include="精确文件名"` 不可靠；output_name 含全路径未去前缀
- **解决：** `--include` 改为 `_DHM_` 前时间戳前缀；`output_name="${output_name##*/}"`
- **涉及文件：** `oss_sync_process.sh`、`oss_extract_process.sh`
- **解决者：** 🧩

### 9. awk index() 误匹配任意位置日期

- **日期：** 2026-06-24
- **原因：** `index($NF, "20260601")` 匹配文件名任意位置 → 改为 `$NF ~ "/20260601"`
- **涉及文件：** `oss_sync_process.sh`、`oss_extract_process.sh`
- **解决者：** 🧩

### 10. OSS 桶配置指向错误桶

- **日期：** 2026-06-24
- **原因：** 脚本配 `ywhcssgdyhxt-znsfkf-dev-1`，实际文件在 `ydxt-2`
- **解决：** 切换凭据和路径为 ydxt-2
- **涉及文件：** `oss_sync_process.sh`、`oss_extract_process.sh`
- **解决者：** 🧩

### 11. oss_ls 中 include_arg 变量不加引号，离线环境 `*` 通配可能失败

- **日期：** 2026-06-24
- **报错：** `ls_out` 有数据（12行），但 `all_remote` 过滤后为空
- **位置：** `oss_ls()` 函数
- **原因：** `include_arg="--include=${date_filter}*"` 作为变量 `${include_arg}` 不加引号展开，`*` 在某些离线环境 shell 中被当作 glob 解释
- **解决：** 去掉 `include_arg` 变量，改为 if/else 直接拼接 `--include="${date_filter}*"`
- **涉及文件：** `oss_sync_process.sh`、`oss_extract_process.sh`
- **解决者：** 🧩

### 12. grep `$` 锚点 + CRLF 行尾导致过滤失败

- **日期：** 2026-06-24
- **报错：** `ossutil ls` 回显 12 行文件列表，但 `grep '\.tar\.gz$'` 过滤后为空
- **位置：** `process_one_date()` → `all_remote=...grep...`
- **原因：** 离线环境 ossutil 输出行尾可能含 `\r`（CRLF）或空格，`$` 锚点无法匹配
- **解决：** 两次 grep 合并为一次 `grep "/${date}.*_DHM_.*\.tar\.gz"`，去掉 `$` 锚点
- **涉及文件：** `oss_sync_process.sh`、`oss_extract_process.sh`
- **解决者：** 🧩

### 13. return 0 导致"跳过"被主循环计为"成功"

- **日期：** 2026-06-24
- **报错：** 脚本输出"无文件，跳过"但最终显示"成功: 1 跳过: 0"
- **位置：** `process_one_date()` 两处 `return 0`
- **原因：** 无文件时 `return 0`，主循环 `if process_one_date; then ((succ++))` 把跳过当作成功
- **解决：** 两处 `return 0` 改为 `return 1`
- **涉及文件：** `oss_sync_process.sh`、`oss_extract_process.sh`
- **解决者：** 🧩

### 14. oss_download 重定向不一致

- **日期：** 2026-06-24
- **报错：** sync 的 ossutil sync 只吞 stdout 不吞 stderr；extract 有双重重定向冗余
- **位置：** `oss_download()` → `>/dev/null`
- **原因：** sync 写 `>/dev/null` 漏了 `2>&1`；extract 有两行重定向
- **解决：** 统一为一行 `>/dev/null 2>&1`
- **涉及文件：** `oss_sync_process.sh`、`oss_extract_process.sh`
- **解决者：** 🧩

### 15. extract 脚本 else fallback `${EXTRACT_TMP}/*` 无 `:?` 保护

- **日期：** 2026-06-24
- **报错：** `/` 目录下文件被移动到 output 解压目录
- **位置：** `process_one_date()` input/output 解压的 `else` 分支 `mv "${EXTRACT_TMP}"/*`
- **原因：** `${EXTRACT_TMP}` 不加 `:?`，变量若意外为空，`/*` 展开为根目录所有文件
- **解决：** 改为 for 循环逐文件 mv + `${EXTRACT_TMP:?}` 防护 + `|| true` 防 set -e 中断
- **涉及文件：** `oss_extract_process.sh`
- **解决者：** 🧩

### 16. set -e 下 for 循环内 mv 无 `|| true`，单个文件失败导致脚本退出

- **日期：** 2026-06-24
- **位置：** `process_one_date()` for 循环内 `mv` 命令
- **原因：** `set -e` 下任何命令失败都会退出，循环内 mv 一个文件失败会中断整个解压
- **解决：** 每个 for 循环内的 `mv` 末尾加 `|| true`，单文件失败继续处理后续文件
- **涉及文件：** `oss_extract_process.sh`
- **解决者：** 🧩

### 17. output_name grep 仍有 `$` 锚点（#12 修复不完整）

- **日期：** 2026-06-24
- **报错：** output 文件检测失效，始终显示"OSS 无对应 output"
- **位置：** `oss_extract_process.sh` L236 / `oss_sync_process.sh` L208
- **原因：** 修复 #12 只去掉了 `all_remote` grep 的 `$`，遗漏了 `output_name` grep 的 `$`
- **解决：** 去掉 `$` → `grep -E "${core_name}_output.*\.tar\.gz"`
- **涉及文件：** `oss_sync_process.sh`、`oss_extract_process.sh`
- **解决者：** 🤖

### 18. extract 脚本 `tar -zxvf` 无错误处理，`set -e` 下失败直接退出

- **日期：** 2026-06-24
- **报错：** tar 解压失败时脚本静默崩溃
- **位置：** `oss_extract_process.sh` L213、L246
- **原因：** `tar -zxvf` 无 `||` 错误处理，`set -e` 下失败终止脚本
- **解决：** `if ! tar -zxvf ...; then log "ERROR" "..."; return 1; fi`
- **涉及文件：** `oss_extract_process.sh`
- **解决者：** 🤖

### 24. output glob 重复 `_output` 目录误迁移

- **日期：** 2026-06-25
- **报错：** output 解压后临时目录全量移至 DHM_OUT
- **位置：** `oss_extract_process.sh` L259
- **原因：** core_name 已含 `_output`，glob 又拼一次 → `xxx_output_output*`
- **解决：** 去掉多余 `_output`，改为 `"${EXTRACT_TMP}/${core_name}"*`
- **涉及文件：** `oss_extract_process.sh`
- **解决者：** 🧩

### 25. `rm -rf ${in_target}` / `${out_target}` 缺 `:?` 防护

- **日期：** 2026-06-25
- **位置：** `oss_extract_process.sh` L225、L261
- **原因：** PREDICT_DIR/busi_date 为空时路径从 `/` 起，可能误删根目录
- **解决：** 改为 `${in_target:?}`、`${out_target:?}`
- **涉及文件：** `oss_extract_process.sh`
- **解决者：** 🧩

---

## 🖥️ Java/X11 类错误（A-离线服务器-安全预控项目）

### 19. no libsigar-amd64-linux.so — 原生库缺失

- **日期：** 2026-06-25
- **报错：** `SigarException: no libsigar-amd64-linux.so in java.library.path`
- **原因：** `.so` 不在 JVM 搜索路径
- **解决：** `-Djava.library.path=/opt/glzz/`
- **涉及文件：** `/opt/glzz/run.sh`
- **解决者：** 🧩

### 20. FileNotFoundException: ClientConfig

- **日期：** 2026-06-25
- **报错：** `ClientConfig（没有那个文件或目录）`
- **位置：** `Config.java:149`
- **原因：** 相对路径，工作目录不是 `/opt/glzz`
- **解决：** 先 `cd /opt/glzz` 再启动
- **涉及文件：** `/opt/glzz/run.sh`
- **解决者：** 🧩

### 21. Can't connect to X11 window server

- **日期：** 2026-06-25
- **报错：** `No protocol specified` / `Can't connect to X11 using ':0'`
- **原因：** gluser 无权连 X 桌面
- **解决：** `xhost +SI:localuser:gluser`
- **解决者：** 🧩

### 22. HeadlessException: No X11 DISPLAY

- **日期：** 2026-06-25
- **报错：** `java.awt.HeadlessException: No X11 DISPLAY variable was set`
- **原因：** `export DISPLAY` 后切会话/sudo 导致变量丢失
- **解决：** `export DISPLAY && java` 同一条命令
- **涉及文件：** `/opt/glzz/run.sh`
- **解决者：** 🧩

### 23. sudo -u gluser 自切用户

- **日期：** 2026-06-25
- **报错：** `run.sh` 内 `sudo -u gluser` 失败
- **原因：** 脚本由 gluser 执行，无需自己切自己
- **解决：** 去掉 `sudo -u`
- **涉及文件：** `/opt/glzz/run.sh`
- **解决者：** 🧩

---

## 文件索引速查

| 错误类型 | 问题数 | 涉及文件 |
|---------|--------|---------|
| MySQL | 4 | `bootstrap.yml`、`/etc/my.cnf`、`V1.0.0__init.sql` |
| Docker | 3 | `docker-compose.yml`、`default.json` |
| Shell | 18 | `oss_sync_process.sh`、`oss_extract_process.sh`、`default.json` |
| Java/X11 | 5 | `/opt/glzz/run.sh` |
| 总计 | 30 | — |

---

*文档由 Reasonix 维护，每次解决新问题后自动追加*
